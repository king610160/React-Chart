version: 0.2

phases:
  install:
    runtime-versions:
      node.js: 18.16
    commands:
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: 'dist'

cache:
  path:
    - 'node_modules/**/*'
