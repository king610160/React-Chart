version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install  # install in root
      - cd frontend 
      - npm install  # install in frontend file
  build:
    commands:
      - pwd   # print right now
      - npm run build

artifacts:
  files:
    - 'frontend/build/**/*'  # accumulate the file in frontend
  base-directory: 'build'

cache:
  path:
    - 'node_modules/**/*'
    - 'frontend/node_modules/**/*'
